{
  "buildCommand": "npm install --legacy-peer-deps && npx vite build --config vite.config.ts",
  "outputDirectory": "dist",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
