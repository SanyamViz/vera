{
  "buildCommand": "cd artifacts/vera-protection && npm install && npx vite build --config vite.config.ts",
  "outputDirectory": "artifacts/vera-protection/dist",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
