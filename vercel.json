{
  "buildCommand": "cd artifacts/vera-protection && npm install --legacy-peer-deps && npx vite build --config vite.config.ts",
  "outputDirectory": "artifacts/vera-protection/dist",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
